# .readthedocs.yaml
# Read the Docs configuration file for building Sphinx docs with Poetry
version: 2

sphinx:
  configuration: docs/source/conf.py

python:
  version: 3.10
  install:
    - method: poetry
      path: .
