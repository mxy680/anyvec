{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Test",
      "type": "shell",
      "command": "poetry run ruff check anyvec --fix && pytest -s --capture=no --disable-warnings"
    }
  ]
}
